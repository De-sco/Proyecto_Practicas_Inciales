// Para crear un tipo de dato juegos

// import { Interface } from "readline";

export interface Game {
    id?: number;
    title?: string;
    description?: string;
    image?: string;
    created_at?: Date
}